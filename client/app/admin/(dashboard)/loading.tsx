import { DashboardSkeleton } from "@/components/ui/SkeletonPresets";

// Route-level loading UI for the embedded admin dashboard.
export default function Loading() {
  return <DashboardSkeleton />;
}
